A repository containing a small example to demonstrate how to install and run MongoDB on Android natively.

## Installation
Just download the repository and run npm install && npm update. 

## Running
Just run node test_db.mjs.

